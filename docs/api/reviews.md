# Reviews API

## GET /api/v1/reviews/user/:userId

Публичный список отзывов о пользователе с cursor-based пагинацией.
Требует только валидного `userId` — авторизация не обязательна.

### Параметры

| Параметр | Тип | Обязательный | Описание |
|----------|-----|--------------|----------|
| `userId` | UUID | ✅ (path) | ID пользователя |
| `limit` | number | ❌ | Количество отзывов на страницу, 1-50, default 20 |
| `cursor` | UUID | ❌ | ID последнего элемента предыдущей страницы |

### Ответ

```json
{
  "items": [
    {
      "id": "uuid",
      "author": {
        "id": "uuid",
        "name": "Иван",
        "avatar": "https://...",
        "rating": 4.8
      },
      "targetRole": "driver",
      "rating": 5,
      "text": "Отличная поездка!",
      "date": "11 августа 2026 г.",
      "tripRoute": "Moscow → SPb"
    }
  ],
  "pagination": {
    "nextCursor": "uuid-of-last-item",
    "hasMore": true,
    "limit": 20
  }
}
```

`nextCursor` — ID последнего элемента текущей страницы; передавайте его как
`cursor` в следующем запросе. `null` означает, что это последняя страница.

### Примеры

**Первая страница:**
```bash
curl /api/v1/reviews/user/{userId}?limit=10
```

**Следующая страница:**
```bash
curl /api/v1/reviews/user/{userId}?limit=10&cursor={nextCursor}
```

### Ошибки

| Код | HTTP | Описание |
|-----|------|----------|
| `NOT_FOUND` | 404 | Пользователь не найден |
| `VALIDATION_FAILED` | 400 | Невалидный формат `cursor` (не UUID) |

Примечание: `cursor` валидного формата, за которым больше нет данных
(включая удалённые записи), возвращает **пустую страницу с `200`**
(`items: []`, `hasMore: false`, `nextCursor: null`) — это не ошибка.

## Known Limitations

### Cursor и удалённые записи

Если запись с `cursor`-ID была удалена между запросами, следующая страница
может пропустить первую существующую запись после удалённой.

**Пример:**
- Записи: `id: 1, 3, 5`
- Cursor: `2` (удалена)
- Запрос: `WHERE ... ORDER BY createdAt DESC SKIP 1` от позиции cursor
- Результат: пропускается `id: 3`, возвращается `id: 5`

**Вероятность:** крайне низкая для отзывов (удаление — редкая операция).
Записи не удаляются из БД при отмене поездок, поэтому на практике
такая ситуация почти не возникает.
